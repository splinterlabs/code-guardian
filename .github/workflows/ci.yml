# CI — Lint and test

name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

permissions:
  contents: read

jobs:
  biome:
    uses: splinterlabs/shared-workflows/.github/workflows/biome.yml@main
    with:
      paths: 'src tests'
