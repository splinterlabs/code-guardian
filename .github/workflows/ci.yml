# CI — Lint and test

name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

permissions:
  contents: read

jobs:
  biome:
    uses: ./.github/workflows/biome.yml
    with:
      paths: 'src tests'
