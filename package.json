{
  "name": "secops-toolkit",
  "version": "1.0.0",
  "description": "Security operations toolkit for code scanning and vulnerability tracking",
  "type": "module",
  "scripts": {
    "test": "bun test",
    "lint": "biome check src tests",
    "lint:fix": "biome check --write src tests",
    "scan": "bun run src/tools/deep-scan.ts",
    "semgrep": "bun run src/tools/semgrep-scan.ts",
    "devbrain": "bun run src/tools/devbrain.ts"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@types/bun": "latest"
  }
}
